<Project>
  <PropertyGroup>
    <!-- Disable fallback packages to avoid cross-platform issues -->
    <DisableImplicitNamespaceImports>true</DisableImplicitNamespaceImports>
  </PropertyGroup>
</Project>
